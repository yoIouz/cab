name: Multi-module CI/CD

on:
  push:
    branches: [ master ]

env:
  GHCR_REGISTRY: ghcr.io
  GHCR_IMAGE_PREFIX: ghcr.io/${{ github.repository }}
  TESTCONTAINERS_RYUK_DISABLED: "true"
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

jobs:
  registry-login:
    runs-on: ubuntu-latest
    steps:
      - name: Login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

  test-modules:
    needs: registry-login
    runs-on: ubuntu-latest

    strategy:
      matrix:
        module: ["passenger-service"]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: 19
          distribution: temurin

      - name: Cache Maven dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.m2/repository
            **/target
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull and retag custom Postgres image
        run: |
          docker pull ghcr.io/yoiouz/repo/cab_db:latest
          docker tag ghcr.io/yoiouz/repo/cab_db:latest cab_db:latest

      - name: Install dependencies for all modules
        run: mvn clean install -DskipTests

      - name: Set DB env
        run: echo "DATABASE_URL=jdbc:postgresql://localhost:5432/test" >> $GITHUB_ENV

      - name: Run tests for ${{ matrix.module }}
        run: mvn clean test -pl passenger-service -Dspring.datasource.url=$DATABASE_URL

  push:
    needs: test-modules
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [ "passenger-service" ]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build ${{ matrix.service }}
        run: |
          docker build \
            --build-arg MODULE_NAME=${{ matrix.service }} \
            -t ghcr.io/yoiouz/repo/${{ matrix.service }}:latest \
            -f ./${{ matrix.service }}/Dockerfile .
      - name: Push ${{ matrix.service }}
        run: |
          docker push ${{ env.REGISTRY_URL }}/${{ matrix.service }}:latest